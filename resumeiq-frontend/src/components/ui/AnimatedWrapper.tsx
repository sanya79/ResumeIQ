/**
 * @deprecated Superseded by the dedicated animation primitives in
 * `@/components/animations` (FadeIn, SlideUp, SlideLeft, SlideRight,
 * ScaleIn, StaggerChildren). Kept as a thin re-export so nothing built
 * against the earlier foundation breaks.
 */
export { SlideUp as AnimatedWrapper } from "@/components/animations/SlideUp";
